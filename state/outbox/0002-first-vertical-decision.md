# Decision → stakeholder: pick the first vertical / product (async — no rush)

**Date:** 2026-07-26 · **Class:** 🛑 decision (async; the studio keeps improving meanwhile)
**Why this matters:** the Productivity *platform* is complete, hardened, and self-verified
(keyless/dry-run). The single highest-leverage move — the one thing that can't be
self-served — is choosing a **first real vertical** to run. That converts an A-grade
platform into an operating venture studio. Everything else keyless is at diminishing
returns.

**What I need from you (answer whenever — this blocks only the "first product" track,
not other work):**
1. A domain/product for the first vertical (what should the studio attempt first?), and
2. the go-live inputs it implies: provider API keys, a monthly budget ceiling, and
   (if the Spokesman channel is wanted) WhatsApp provisioning.

**To make it easy to react to, some archetypes (pick / adapt / reject):**
- **Content/media micro-product** (e.g. a niche newsletter or short-form video pipeline)
  — exercises the coding-worker + research + a `video_audit`-style verify checker.
- **Software micro-SaaS / tool** — exercises the coding-worker (opencode/cursor-in-sandbox),
  PM decompose, reviewer, deploy-gated 🔴.
- **Research/analysis service** — exercises Researcher + memory + grounding-gated reporting.
- **Internal automation** (something you personally want automated) — lowest external risk,
  fastest real-traffic signal to validate the learning/efficacy loops.

**Defaults if you don't answer:** nothing is auto-started (a first product + real spend is
your call). I keep hardening/improving the platform and will pick this up the moment you
answer. Provider-key specifics live in `0001-model-key-request.md`; economics in
`docs/cost-model.md`; launch steps in `docs/go-live.md` (`python -m runtime.readiness`).
